# Legacy Contract Path

This directory is no longer the canonical source of truth for DV contracts.

## Canonical root

Use:

- `C:\dev\AspectNova\contracts`

## Rule

Do not add new canonical contract files here.
Consumers must resolve contract paths through the shared contract resolver.